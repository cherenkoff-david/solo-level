# Описание версий и документация проекта Solo Level

Этот файл описывает историю изменений и структуру проекта, с указанием даты и времени.

---

## [2026-02-13 01:06] - Начальная фиксация структуры и перевод документации

### 📂 Структура Проекта (Monorepo)

- **`client/`**: Фронтенд (React + Vite)
- **`server/`**: Бэкенд (Node.js + Express)
- **`shared/`**: Общий код (типы, константы)

### 🖥️ Client (`/client`)

Фронтенд с использованием "Dark RPG" эстетики и glassmorphism.

#### `src/components/`
- **`Layout.jsx`**: Основная обертка приложения. Содержит боковую панель (Sidebar) с навигацией, профилем пользователя и динамическим отображением версии. Логика сворачивания сайдбара.
- **`Dashboard.jsx`**: Главная страница ("Обзор"). Отображает Bento Grid со статистикой (Уровень, Опыт, HP, Монеты), задачи на сегодня и активности.
- **`TaskBoard.jsx`**: Управление "Миссиями" (Tasks). Доска в стиле канбан с выдвижной панелью создания задач.
- **`HabitTracker.jsx`**: Управление "Протоколами" (Habits). Сетка карточек привычек с отслеживанием серий (streak).
- **`ProgressPage.jsx`**: Страница "Анализ" с подробной статистикой.
- **`BalanceWheel.jsx`**: Компонент лепестковой диаграммы (Radar Chart) для визуализации RPG характеристик (Сила, Ловкость, Интеллект и т.д.).
- **`BackgroundElements.jsx`**: Рендеринг анимированных цветных "пятен" (blobs) на фоне.
- **`ui/`**: Переиспользуемые UI компоненты (кнопки, инпуты, карточки).

#### `src/pages/`
- **`Login.jsx`**: Экран входа (с возможностью гостевого доступа).
- **`Register.jsx`**: Экран регистрации.
- **`SettingsPage.jsx`**: Страница настроек. Выбор языка (флаги) и другие параметры.

#### Конфигурация
- **`vite.config.js`**: Конфиг Vite. Содержит кастомный плагин для автоматического инкремента версии в `version.json` при каждом сохранении файла.
- **`version.json`**: Хранит текущую версию (например, `0.1.1`). Обновляется автоматически.
- **`src/styles/`**:
    - `global.css`: Глобальные стили.
    - `theme.css`: Дизайн-токены (цвета, переменные, glassmorphism).

### 🔌 Server (`/server`)

Монолитный бэкенд на Node.js/Express с базой данных SQLite.

#### `index.js`
Точка входа. Настройка Express сервера, CORS, парсинг JSON.

#### `routes/`
- **`auth.js`**: Эндпоинты аутентификации (`/login`, `/register`, `/me`). Использует JWT.
- **`game.js`**: Логика игры (получение статистики, задач, привычек).

#### `db/`
- **`game.db`**: Файл базы данных SQLite.
- **`index.js`**: Подключение к БД через `better-sqlite3` с режимом WAL.
- **`schema.sql`**: SQL схема таблиц (`users`, `tasks`, `habits`...).

#### `middleware/`
- **`auth.js`**: Проверка JWT токенов для защиты маршрутов.

### ✨ Ключевые возможности

1.  **Динамическое версионирование**: Плагин Vite автоматически увеличивает патч-версию (0.1.0 -> 0.1.1) при каждом изменении файлов в `client/src`. Версия сразу отображается в футере сайдбара.
2.  **База данных**: SQLite (`server/db/game.db`). Можно подключиться через "SQLite Viewer" в VS Code или "DB Browser for SQLite".
3.  **Сворачиваемый сайдбар**: Двойной клик по правой границе сайдбара сворачивает его до режима иконок (ширина 80px).

---
